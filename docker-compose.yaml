version: "3"

services:
#  mongodb:
#    image: mongo
#    container_name: mongodb
#    ports:
#      - 27017:27017
#    volumes:
#      - /opt/mongo/data:/data/db
#    restart: always

  sql_db:
    image: postgres
    volumes:
      - ./data/db:/var/lib/postgresql/data
    ports:
      - "8901:5432"
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_HOST=172.18.0.2
      - POSTGRES_PORT=5432

  backend:
    entrypoint: hypercorn -k uvloop --bind="localhost:8081" --debug --reload main
    restart: always
    container_name: backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    hostname: backend
    links:
      - sql_db
    depends_on:
      - sql_db
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_HOST=172.18.0.2
      - POSTGRES_PORT=5432
    volumes:
      - ./storage:/storage
